[package]
name = "mold-tests"
version = "2.42.1"
edition = "2021"
description = "Test support for mold's shell test suite"
license = "MIT"
publish = false

[lib]
path = "lib.rs"

[dependencies]
libc = "0.2"
